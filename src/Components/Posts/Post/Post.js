import React from 'react';

const Post = (props) => {
  return ( 
    <h1>{props.info.title}: {props.info.post}</h1>
   );
}
 
export default Post;

const prettier = (props) =>{
  console.log('sdflkj')}